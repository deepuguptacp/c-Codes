#include<stdio.h>
int main(){
	double a, b, area;
	printf("Enter the length of equal sides of the triangle : 																");
	scanf("%lf", &a);
	printf("Enter the length of unequal side of triangle : ");
	scanf("%lf", &b);0
	//i m not getting how to put its formulae in it	
	//@TODO
	// it's herone area = sqrt(s*(s-a)*(s-b)*(s-c)), s=(a+b+c)/2
	return 0;
}
