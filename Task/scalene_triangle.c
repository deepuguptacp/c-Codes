#include<stdio.h>
int main(){
	double a, b, c, area;
	printf("Enter the length of the sides of the triangle : 																");
	scanf("%lf %lf %lf", &a, &b, &c);
	//@TODO
	// it's herone area = sqrt(s*(s-a)*(s-b)*(s-c)), s=(a+b+c)/2
	return 0;
}
