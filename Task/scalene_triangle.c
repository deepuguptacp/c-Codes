#include<stdio.h>
int main(){
	double a, b, Area;
	printf("Enter the length of equal sides of the triangle : 																");
	scanf("%lf", &a);
	printf("Enter the length of unequal side of triangle : ");
	scanf("%lf", &b);0
	//i m not getting how to put its formulare in it	
	return 0;
}
